# -*- mode: snippet -*-
# name: use-package hydra only
# key:use-ho
# --
  :mode-hydra
  ($1-mode
   (:hint nil :foreign-keys warn :title "title" :quit-key "q" )
   ("${2:Subtitle}"
    (("${3:kb}" ${4:func} "${5:Desc}"))))