# -*- mode: snippet -*-
# name: read file
# key: rf
# --
path = "${1:/tmp/test.txt}"
try:
    f = open(path,'r')
    f.seek(0)    # set cursor at top of the file ( it was at the end after write)
    ${2:content} = f.read()
    f.close()
except Exception:
    print("Couldn't read the file")
